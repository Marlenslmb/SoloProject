import { Grid } from '@material-ui/core';
import React from 'react';
import Carousel from '../Carousel/Carousel';

const Main = (props) => {

    return (
        <Grid>
            <Carousel />
        </Grid>
    );
};

export default Main;